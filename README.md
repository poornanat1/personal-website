# Poorna Natarajan — Portfolio

A responsive, one-page portfolio built with React, TypeScript, Vite, and
Tailwind CSS.

## Development

Requires Node.js 20 or later.

```bash
npm install
npm run dev
```

## Production

```bash
npm run build
npm run preview
```

The production build is written to `dist/`.
